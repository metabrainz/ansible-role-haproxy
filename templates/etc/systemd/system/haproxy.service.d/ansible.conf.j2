#jinja2: trim_blocks: True, lstrip_blocks: True
# {{ ansible_managed }}
{% import "systemd-macros.j2" as macros with context %}
{% if _haproxy_systemd_overrides %}

{%   for section, config in _haproxy_systemd_overrides|dictsort %}
{{ macros.systemd_section(config, section) -}}
{%     if not loop.last %}

{%     endif %}
{%   endfor %}
{% endif %}
